const Child = () => {

    const parentDataInChild = '';

    return (
        <>
            <h1>Child component</h1>
            <p>Parent data in child component:
                {parentDataInChild}</p>
        </>
    );
};

export default Child;